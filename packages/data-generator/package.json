{
  "name": "@claimguard/data-generator",
  "private": true,
  "scripts": {
    "build": "uv run python -m compileall src",
    "lint": "uv run ruff check src tests --exit-zero",
    "test": "uv run claimguard-generate --config generation_config.yaml && uv run pytest tests --cov=src/claimguard --cov-report=xml"
  }
}
