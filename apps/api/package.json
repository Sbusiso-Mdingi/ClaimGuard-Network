{
  "name": "@claimguard/api",
  "private": true,
  "type": "module",
  "dependencies": {
    "@azure/identity": "^4.5.0",
    "@azure/storage-blob": "^12.25.0",
    "@claimguard/shared-schema": "workspace:*",
    "@claimguard/database": "workspace:*",
    "@hono/node-server": "^1.14.0",
    "@sentry/node": "^8.0.0",
    "@trpc/server": "^11.0.0",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.0.0",
    "newrelic": "^11.0.0",
    "zod": "^3.24.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R src/* dist/",
    "lint": "node -e \"import('./src/backend.js')\"",
    "test": "node --test tests/*.test.js",
    "test:backend": "node --test tests/*.test.js",
    "test:sentry": "node src/sentry-smoke.js",
    "dev:backend": "node src/backend-server.js",
    "smoke:newrelic": "node src/newrelic-smoke.cjs"
  }
}
