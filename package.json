{
  "name": "claimguard-network",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "report:worker": "pnpm --dir services/report-producer exec claimguard-produce-report worker --once",
    "report:producer:test": "pnpm --dir services/report-producer test"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
