name: claimguard-api-deploy-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the production deployment boundary
        run: |
          echo "This legacy direct API deployment workflow is retired."
          echo "Use the ci workflow's manual production dispatch with:"
          echo "  expected_main_sha=<exact validated 40-character main SHA>"
          echo "  production_confirmation=DEPLOY_PRODUCTION"
