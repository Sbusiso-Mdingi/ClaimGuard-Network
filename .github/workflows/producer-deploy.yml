name: report-producer-deploy-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the deployment boundary
        run: |
          echo "This legacy workflow is intentionally retired."
          echo "It used the removed ML_INFERENCE_URL/schema-10 seam."
          echo "Use the locally validated Bicep plan in .azure/deployment-plan.md."
          exit 1
