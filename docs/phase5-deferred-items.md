# Deferred Items (Post-Phase 4)

Dashboard Demo Mode:

- Add a UI toggle allowing investigators to switch between static data and replay/live synthetic data.
- Default to live replay for demonstrations.
- The toggle should affect only presentation/polling behaviour and must not change detection engine outputs.
